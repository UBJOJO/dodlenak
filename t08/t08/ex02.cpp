#include <stdio.h>

int main() {
	/*char a;

	printf("문자입력: ");
	scanf("%c", &a);
	printf("%c의 ASCII 값: %d", a, a);
	*/

	int a;
	printf("숫자입력: ");
	scanf("%d", &a);
	printf("%d의 ASCII 변환 값: %c", a, a);
}